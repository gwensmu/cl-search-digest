# To run the webapp locally:

        ruby app.rb -p 3000

# To email yourself

    Install and configure sendmail on your host. 

    ```
    > $ ruby app/mailer.rb
    > Delivered an email!
    ```
